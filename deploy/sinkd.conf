[owner]
name = "tony"
key = "ssh-key"

[[users]]
name = "tony"
address = "user1@host"
ssh_key = "ssh-key"

[[users]]
name = "atlas"
address = "user2@host"
ssh_key = "ssh-key2"

# directories to watch
[[anchor_points]]
path = "path/to/stuff"
users = ["bob", "tony"]
interval = 5  # in seconds
excludes = [
"first_dir", 
"second_dir"
]

[[anchor_points]]
path = "/path/to/awesomeness"
users = ["tony"]
interval = 60 # in secs (minute for default)
excludes = [
  "n/a"
]
