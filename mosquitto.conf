# Basic Mosquitto Configuration

# Listen on port 1883 on all network interfaces
listener 1883 0.0.0.0

# Allow anonymous connections (no username/password required)
allow_anonymous true

# Disable persistence (optional, keeps the broker stateless)
persistence false
