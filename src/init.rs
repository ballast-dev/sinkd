pub fn server(verbosity: u8) {
    // somehow designate that this is the server
    // write to a file? 
    println!("init the server! v:{}", verbosity)
}

pub fn client(verbosity: u8) {
    println!("init the client! v:{}", verbosity)
}